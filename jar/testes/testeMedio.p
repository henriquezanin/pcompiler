program fatorial;
var x, aux, fat: integer;
begin
        read(x);
        fat := 1;
        for aux := 1 to x do
        begin
                fat := fat*aux;
        end;
        write(fat);
        if i >= 0 then;
end.
