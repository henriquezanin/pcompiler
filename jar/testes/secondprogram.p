program demaior;

var idade, demenor, demaior: integer;

begin

        read(idade);
        demenor := -1;
        demaior := 1;

        if idade > 18 then
	begin
            write(demaior);
            write(demaior);
        end
        else
            write(demenor);
end.
